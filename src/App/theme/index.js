import SWATCHES from "./SWATCHES";

const theme = {
    primary: SWATCHES.denim,
    secondary: SWATCHES.flushOrange,
    accent: SWATCHES.gold,
    dark: SWATCHES.cinder,
    light: SWATCHES.ecruWhite,
}

export default theme;